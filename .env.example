# ============================================================================
# Root Environment Configuration (for Development Tooling Only)
# ============================================================================
# ⚠️  IMPORTANT: This file is for local development tooling only (Kanel, Supabase CLI)
# ⚠️  For runtime app configuration, see app-specific .env files:
#     - apps/backend/.env.example
#     - apps/frontend/smb/.env.example
#     - apps/frontend/investor/.env.example
#
# Copy this file to .env for local development

# ============================================================================
# Database Configuration (for Kanel type generation)
# ============================================================================
# These values connect to local Supabase instance for type generation only
# Kanel reads from this file to generate TypeScript types from your database schema

# Local Supabase PostgreSQL connection
POSTGRES_HOST=localhost
POSTGRES_PORT=54322
POSTGRES_DATABASE=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# ============================================================================
# Supabase Configuration (for Supabase CLI)
# ============================================================================
# Get these from Supabase Dashboard: https://app.supabase.com
# Used by Supabase CLI for migrations and local development

# Local Supabase URLs
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Production Supabase URLs (when using Supabase CLI with remote)
# NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-production-anon-key
